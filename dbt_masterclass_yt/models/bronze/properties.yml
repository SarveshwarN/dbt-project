version: 2

models:
  - name: bronze_customer #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: view

  - name: bronze_date
    config:
      materialized: view

